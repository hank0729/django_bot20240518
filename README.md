# django Line Bot

```python
1.python -m venv venv

2.venv\Script\activate

3.pip install -r requirements.txt

4.python manage.py runserver

5. ./redis-server

6.celery -A myproject worker --loglevel=info

7.celery -A myproject beat --loglevel=info

ngrok http 8000
```
